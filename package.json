{
  "name": "secret-chat",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend install --production=false && npm --prefix frontend run build"
  }
}
